swagger: '2.0'
info:
  version: '1.0'
  title: "HUBusivi"
  description: REST Api per gestire gli esami
  license:
    name: MIT
    url: https://github.com/apiaryio/polls-api/blob/master/LICENSE
host: ingegneriasoftwa2.herokuapp.com
basePath: /
schemes:
- https
consumes:
- application/json
produces:
- application/json 
paths:
  
  /students:
    post:
      tags: 
      - "Users"
      summary: Add a student on the database
      operationId: addStudent
      parameters:
        - name: student_attributes
          in: query
          description: data of the student
          type: string
          x-example: "Pinco pallino"
      responses:
        400:
          description: Bad request
        404:
          description: Resources not found
        200:
          description: Successful response
  
  /students/matricola:
    delete:
      summary: Remove a student from the database
      operationId: addStudent
      parameters:
        - name: matricola
          in: query
          description: Matricola of the student that I want delete
          type: string
          x-example: "Pinco pallino"
      responses:
        400:
          description: Bad request
        404:
          description: Resources not found
        200:
          description: Successful response
  
  /assignment:
    post: 
        tags: 
        - "Assignment"
        
        summary: Add an Assignment(only teacher)
        operationId: AddAssignment
        parameters:
          - name: Name 
            in: query
            description: Name of assignment
            type: string
            x-example: "Compito Gennaio"
          - name: Date
            in: query
            description: Date of assignment
            type: string
            x-example: "11/11/2018"
          - name: Delivery date
            in: query
            description: Delivery date of assignment
            type: string
            x-example: "12/12/2018"
          - name: Text
            in: query
            description: Text of the assignment
            type: string
            x-example: "Voglio un programma in javascript che faccia..."
          
        responses:
          400:
            description: Bad request
          404:
            description: Resources not found
          200:
            description: Successful response

  /assignment/assignmentId:  
    delete:
      summary: Remove an assignment
      operationId: removeAssignment
      parameters:
        - name: Name
          in: query
          description: name of assignment
          type: string
          x-example: "Compito Gennaio"
        - name: Date
          in: query
          description: Date of assignment
          type: string
          x-example: "11/11/2018"
        - name: Delivery date
          in: query
          description: Delivery date of assignment
          type: string
          x-example: "12/12/2018"
        - name: assignmentId
          in: query
          type: integer
          x-example: "113425"
      responses:
        400:
          description: Bad request
        404:
          description: Resources not found
        200:
          description: Successful response  
  
    put:
      summary: Update an assignment
      operationId: updateAssignment
      parameters:
        - name: Name
          in: query
          description: name of assignment
          type: string
          x-example: "Compito Gennaio"
      responses:
        400:
          description: Bad request
        404:
          description: Resources not found
        200:
          description: Successful response
          
  /marks:
    
    post:
      tags:
      - "Marks"
      summary: Add a Mark (only teacher)
      operationId: addMark
      parameters:
        - name: mark
          in: query
          description: voto da aggiungere
          type: integer
          x-example: 22
        - name: matricola
          in: query
          description: matricola of the student 
          type: integer
          x-example: 123456
        - name: course_id
          in: query
          description: course id of the mark
          type: integer
          x-example: 113355
      responses:
        400:
          description: Bad request
        404:
          description: Resources not found
        200:
          description: Successful response
    
    delete:
      summary: Remove a Mark of a specific exam of a student (only teacher)
      operationId: removeMark
      parameters:
        - name: matricola
          in: query
          description: matricola of the student
          type: integer
          x-example: 123456
        - name: course_id
          in: query
          description: course id of the mark
          type: integer
          x-example: 113355
      responses:
        400:
          description: Bad request
        404:
          description: Resources not found
        200:
          description: Successful response

    put:
      summary: Update a mark of a specific exam of a student (only teacher)
      operationId: updateMark
      parameters:
        - name: mark
          in: query
          description: mark to update
          type: integer
          x-example: 22
        - name: matricola
          in: query
          description: matricola of the student
          type: integer
          x-example: 123456
        - name: course_id
          in: query
          description: course id of the mark
          type: integer
          x-example: 113355
      responses:
        400:
          description: Bad request
        404:
          description: Resources not found
        200:
          description: Successful response
  /marks/courseId:
    get:
      summary: Get your marks of a specific course
      operationId: updateMark
      parameters:
        - name: mark
          in: query
          description: mark to update
          type: integer
          x-example: 22
        - name: matricola
          in: query
          description: matricola of the student
          type: integer
          x-example: 123456
        - name: course_id
          in: query
          description: course id of the mark
          type: integer
          x-example: 113355
      responses:
        400:
          description: Bad request
        404:
          description: Resources not found
        200:
          description: Successful response
          
  /marks/avg:
    post:
      description: >-
        Calculate the marks average about a specific exam of a course.
      summary: Average course marks (only teacher)
      operationId: addMark
      parameters:
        - name: course_id
          in: query
          description: Course which I want to get the average of marks
          type: integer
          x-example: 113355
      responses:
        400:
          description: Bad request
        404:
          description: Resources not found
        200:
          description: Marks average
          
  
  /courses:
    put:
      summary: Add a course(teacher)/Join a course(student)
      operationId: addCourse
      parameters:
        - name: course data
          in: query
          description: course data
          type: string
          x-example: 'Ingegneria del software 2'
      responses:
        400:
          description: Bad request
        404:
          description: Resources not found
        200:
          description: Successful response
  
    get:
      tags:
      - "Courses"
      summary: Get the list of all your courses
      operationId: coursesList
      parameters:
        - name: matricola
          in: query
          description: matricola of the user
          type: integer
          x-example: 123456
      responses:
        400:
          description: Bad request
        404:
          description: Resources not found
        200:
          description: List of all your courses


  /courses/courseId:  
    delete:
      summary: Remove a course(teacher only)/Leave a course(student only)
      operationId: removeCourse
      parameters:
        - name: course_id
          in: query
          description: Course which I want delete/leave
          type: integer
          x-example: 113355
      responses:
        400:
          description: Bad request
        404:
          description: Resources not found
        200:
          description: Successful response
  
    post:
      summary: Update a Course(teacher only)
      operationId: updateMark
      parameters:
        - name: course info
          in: query
          description: course info
          type: string
          x-example: 'Ingegneria del software - 2'
        - name: course_id
          in: query
          description: course id
          type: integer
          x-example: 113355
      responses:
        400:
          description: Bad request
        404:
          description: Resources not found
        200:
          description: Successful response